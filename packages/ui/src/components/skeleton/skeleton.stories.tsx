import React from "react";
import type { StoryDefault, Story } from "@ladle/react";
import { } from "./skeleton";
import '../../tailwind.css'

export default {
    title: "[skeleton] primary",
} satisfies StoryDefault;
export const skeletonStory: Story = () => (
    <>sample story</>
);