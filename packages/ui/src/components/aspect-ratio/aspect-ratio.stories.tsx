import React from "react";
import type { StoryDefault, Story } from "@ladle/react";
import { } from "./aspect-ratio";
import '../../tailwind.css'

export default {
    title: "[aspect-ratio] primary",
} satisfies StoryDefault;
export const aspectratioStory: Story = () => (
    <>sample story</>
);
