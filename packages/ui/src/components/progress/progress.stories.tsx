import React from "react";
import type { StoryDefault, Story } from "@ladle/react";
import { } from "./progress";
import '../../tailwind.css'

export default {
    title: "[progress] primary",
} satisfies StoryDefault;
export const progressStory: Story = () => (
    <>sample story</>
);