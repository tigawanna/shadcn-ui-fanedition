import React from "react";
import type { StoryDefault, Story } from "@ladle/react";
import { } from "./hover-card";
import '../../tailwind.css'

export default {
    title: "[hover-card] primary",
} satisfies StoryDefault;
export const hovercardStory: Story = () => (
    <>sample story</>
);
